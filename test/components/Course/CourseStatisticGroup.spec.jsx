/*import React from "react";
import { shallow } from "enzyme";

import CourseStatisticGroup from "../../../app/components/Course/CourseStatisticGroup.jsx";

describe("COMPONENT: CourseStatisticGroup", () => {
    describe("#creditPoints", () => {
        const wrapper = shallow(
            <CourseStatisticGroup
                creditPoints={72}
                cost={15600}
                />
        );

        expect(wrapper.find(".statistic .value").text().trim()).to.equal("72");
    });
});
*/
